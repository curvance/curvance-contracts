# Unitroller.sol

* `Unitroller` delegates calls to the `Comptroller` implementation to execute
* Stores the addresses of `admin` and `comptrollerImplementation`
  * Contains functions to set a new `admin` or `comptrollerImplementation`
